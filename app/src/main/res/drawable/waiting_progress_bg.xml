<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 实心,就是填充的意思, android:color指定填充的颜色-->
    <solid android:color="@color/waiting_bg"/>
    
    <!-- 间隔 -->
    <padding android:left="5.0dp" android:right="5.0dp" android:top="5.0dp" android:bottom="5.0dp"/>
    
    <!-- 圆角,android:radius为角的弧度，值越大角越圆 -->
    <corners android:radius="5.0dp"/>
    
    <!-- 描边,android:width="2dp" 描边的宽度，android:color 描边的颜色 
		还可以把描边弄成虚线的形式，设置方式为：
		android:dashWidth="5dp" 
		android:dashGap="3dp"
		其中android:dashWidth表示'-'这样一个横线的宽度，android:dashGap表示之间隔开的距离-->
    <stroke android:width="0.0dp" android:color="@color/waiting_bg"/>
    
    <!-- gradient: 渐变,android:startColor和android:endColor分别为起始和结束颜色，
    	android:angle是渐变角度，必须为45的整数倍。
		另外渐变默认的模式为android:type="linear"，即线性渐变，
		可以指定渐变为径向渐变，android:type="radial"，径向渐变需要指定半径android:gradientRadius="50" -->
</shape>
